package JsonRPC;

import java.util.List;
import java.util.Map;


public class blockchain_Info {

	/*
	public static String getCoinBlockHash(String method, Map<String, Object> params, Object id) {
		  // Create the response (note that the JSON-RPC ID must be echoed back)
        String result = "payment-id-001";
        JSONRPC2Response respOut = new JSONRPC2Response(result, reqIn.getID());

        // Serialise response to JSON-encoded string
        jsonString = respOut.toString();
		return null;
	}
	
	public static String getCoinBlockNumber(String method, Map<String, Object> params, Object id) {
		  // Create the response (note that the JSON-RPC ID must be echoed back)
        String result = "payment-id-001";
        JSONRPC2Response respOut = new JSONRPC2Response(result, reqIn.getID());

        // Serialise response to JSON-encoded string
        jsonString = respOut.toString();
		return null;
	}

	public static String getCoinBlockInfo(String method, Map<String, Object> params, Object id) {
		  // Create the response (note that the JSON-RPC ID must be echoed back)
        String result = "payment-id-001";
        JSONRPC2Response respOut = new JSONRPC2Response(result, reqIn.getID());

        // Serialise response to JSON-encoded string
        jsonString = respOut.toString();
		return null;
	}
	
	public static String getPrevCoinBlockInfo(String method, Map<String, Object> params, Object id) {
		  // Create the response (note that the JSON-RPC ID must be echoed back)
        String result = "payment-id-001";
        JSONRPC2Response respOut = new JSONRPC2Response(result, reqIn.getID());

        // Serialise response to JSON-encoded string
        jsonString = respOut.toString();
		return null;
	}
	

	
	public static String getChainHeight(String method, Map<String, Object> params, Object id) {
		  // Create the response (note that the JSON-RPC ID must be echoed back)
        String result = "payment-id-001";
        JSONRPC2Response respOut = new JSONRPC2Response(result, reqIn.getID());

        // Serialise response to JSON-encoded string
        jsonString = respOut.toString();
		return null;
	}
	

	public static String getLatestCoinBlock(String method, Map<String, Object> params, Object id) {
		  // Create the response (note that the JSON-RPC ID must be echoed back)
        String result = "payment-id-001";
        JSONRPC2Response respOut = new JSONRPC2Response(result, reqIn.getID());

        // Serialise response to JSON-encoded string
        jsonString = respOut.toString();
		return null;
	}
	
	
	public static List<String> getAllCoinBlockTransactions(String method, Map<String, Object> params, Object id) {
		  // Create the response (note that the JSON-RPC ID must be echoed back)
        String result = "payment-id-001";
        JSONRPC2Response respOut = new JSONRPC2Response(result, reqIn.getID());

        // Serialise response to JSON-encoded string
        jsonString = respOut.toString();
		return null;
	}
	
	public static String getCoinTransaction(String method, Map<String, Object> params, Object id) {
		  // Create the response (note that the JSON-RPC ID must be echoed back)
        String result = "payment-id-001";
        JSONRPC2Response respOut = new JSONRPC2Response(result, reqIn.getID());

        // Serialise response to JSON-encoded string
        jsonString = respOut.toString();
		return null;
	}
	*/
}
