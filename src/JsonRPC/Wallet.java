package JsonRPC;

import java.util.Map;


public class Wallet {

	/*public String createAccount() {
		  // Create the response (note that the JSON-RPC ID must be echoed back)
		JSONObject jsonObject = new JSONObject();
		jsonObject.put("key", "value");
        String result = "payment-id-001";
        JSONRPC2Response respOut = new JSONRPC2Response(result, reqIn.getID());

        // Serialise response to JSON-encoded string
        jsonString = respOut.toString();
		return null;
	}
	
	public static String getPublicKey(String method, Map<String, Object> params, Object id) {
		  // Create the response (note that the JSON-RPC ID must be echoed back)
        String result = "payment-id-001";
        JSONRPC2Response respOut = new JSONRPC2Response(result, reqIn.getID());

        // Serialise response to JSON-encoded string
        jsonString = respOut.toString();
		return null;
	}
	
	public static String getMintAddress(String method, Map<String, Object> params, Object id) {
		  // Create the response (note that the JSON-RPC ID must be echoed back)
        String result = "payment-id-001";
        JSONRPC2Response respOut = new JSONRPC2Response(result, reqIn.getID());

        // Serialise response to JSON-encoded string
        jsonString = respOut.toString();
		return null;
	}
	
	public static String getAccountCoinNonce(String method, Map<String, Object> params, Object id) {
		  // Create the response (note that the JSON-RPC ID must be echoed back)
        String result = "payment-id-001";
        JSONRPC2Response respOut = new JSONRPC2Response(result, reqIn.getID());

        // Serialise response to JSON-encoded string
        jsonString = respOut.toString();
		return null;
	}
	
	public static String getAccountPackNonce(String method, Map<String, Object> params, Object id) {
		  // Create the response (note that the JSON-RPC ID must be echoed back)
        String result = "payment-id-001";
        JSONRPC2Response respOut = new JSONRPC2Response(result, reqIn.getID());

        // Serialise response to JSON-encoded string
        jsonString = respOut.toString();
		return null;
	}
	
	public static String getCoinBalance(String method, Map<String, Object> params, Object id) {
		  // Create the response (note that the JSON-RPC ID must be echoed back)
        String result = "payment-id-001";
        JSONRPC2Response respOut = new JSONRPC2Response(result, reqIn.getID());

        // Serialise response to JSON-encoded string
        jsonString = respOut.toString();
		return null;
	}
	
	public static String CreateCoinTransaction(String method, Map<String, Object> params, Object id) {
		  // Create the response (note that the JSON-RPC ID must be echoed back)
        String result = "payment-id-001";
        JSONRPC2Response respOut = new JSONRPC2Response(result, reqIn.getID());

        // Serialise response to JSON-encoded string
        jsonString = respOut.toString();
		//return tx status
		return null;
	}
	*/
	
}
